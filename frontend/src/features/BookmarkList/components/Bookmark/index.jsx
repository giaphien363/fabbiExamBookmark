import React from 'react'

const Bookmark = ({id}) => {
    return (
        <div style={{marginTop:"10px"}}>
            bookmark  {id}
        </div>
    )
}

Bookmark.propTypes = {}

export default Bookmark
